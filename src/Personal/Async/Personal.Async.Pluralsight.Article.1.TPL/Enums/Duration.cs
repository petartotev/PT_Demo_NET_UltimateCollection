namespace Personal.Async.Pluralsight.Article._1.TPL.Enums
{
    // Define the duration of a note in units of milliseconds.
    public enum Duration
    {
        WHOLE = 1600,
        HALF = WHOLE / 2,
        QUARTER = HALF / 2,
        EIGHTH = QUARTER / 2,
        SIXTEENTH = EIGHTH / 2,
    }
}
